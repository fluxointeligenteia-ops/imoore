const http = require('http'); const data = JSON.stringify({email:'crm@imoore.com', senha:'crm@2026'}); const opts = { host:'127.0.0.1', port:3001, path:'/api/auth/login', method:'POST', headers:{'Content-Type':'application/json','Content-Length':Buffer.byteLength(data)} }; const req=http.request(opts,(res)=>{let body=''; res.on('data',(chunk)=>body+=chunk); res.on('end',()=>{ console.log(res.statusCode, body); }); }); req.on('error',(err)=>{ console.error('ERR', err.message); }); req.write(data); req.end();
